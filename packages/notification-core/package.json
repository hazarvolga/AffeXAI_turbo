{
	"name": "@affex/notification-core",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Email, Slack, Telegram, in-app notification abstraction (Layer 2 — OPTIONAL_LATER)",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/",
		"test": "vitest run --passWithNoTests",
		"clean": "rimraf dist"
	},
	"dependencies": {
		"@affex/shared-types": "workspace:*",
		"@slack/web-api": "^7.8.0",
		"resend": "^4.1.2",
		"telegraf": "^4.16.3"
	},
	"devDependencies": {
		"@types/node": "^22.13.1",
		"typescript": "^5.7.3",
		"vitest": "^2.1.8"
	},
	"layer": "OPTIONAL_LATER"
}
