{
	"name": "@affex/feature-flags",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Lightweight feature flag client with in-memory store and rule-based evaluation",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/",
		"test": "vitest run --passWithNoTests",
		"clean": "rimraf dist"
	},
	"devDependencies": {
		"typescript": "^5.7.3",
		"vitest": "^2.1.8"
	},
	"layer": "OPTIONAL_LATER"
}
