{
	"name": "@affex/billing-core",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Stripe / Lemon Squeezy billing abstraction (Layer 2 — OPTIONAL_LATER)",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/",
		"test": "vitest run --passWithNoTests",
		"clean": "rimraf dist"
	},
	"dependencies": {
		"@affex/shared-types": "workspace:*",
		"@lemonsqueezy/lemonsqueezy.js": "^3.0.0",
		"stripe": "^17.0.0"
	},
	"devDependencies": {
		"typescript": "^5.7.3",
		"vitest": "^2.1.8"
	},
	"layer": "OPTIONAL_LATER"
}
